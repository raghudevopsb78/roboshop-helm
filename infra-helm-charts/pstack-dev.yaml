alertmanager:
  ingress:
    enabled: true
    ingressClassName: nginx
    annotations:
      external-dns.alpha.kubernetes.io/hostname: alertmanager-dev.rdevops6a.online
    hosts:
      - alertmanager-dev.rdevops6a.online

grafana:
  ingress:
    enabled: true
    ingressClassName: nginx
    annotations:
      external-dns.alpha.kubernetes.io/hostname: grafana-dev.rdevops6a.online
    hosts:
      - grafana-dev.rdevops6a.online

prometheus:
  ingress:
    enabled: true
    ingressClassName: nginx
    annotations:
      external-dns.alpha.kubernetes.io/hostname: prometheus-dev.rdevops6a.online
    hosts:
      - prometheus-dev.rdevops6a.online
